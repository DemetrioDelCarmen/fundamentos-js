var edad = 19
var peso = 75
peso -= 2

var sandwich = 1
peso += sandwich

var jugarFutbol = 3
peso -= jugarFutbol
var precioVino=200.02

    var total = Math.round(precioVino *100*3)/100
    var totalStr = total.toFixed(3)
    var total2  = parseFloat(totalStr)
    var pizza = 8
    var personas =3
    var totalPersonas = pizza /personas
    // var totalPersonasAux = Math.round(totalPersonas * 100*3)/100
    var totalPreciso = totalPersonas.toFixed(2)